spring:
  application:
    name: checkcode
  cloud:
    nacos:
      server-addr: localhost:8848
  profiles:
    active: dev
  redis:
    host: localhost
    port: 6379


server:
  servlet:
    context-path: /checkcode
  port: 63075
